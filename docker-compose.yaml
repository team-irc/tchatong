version: "3.3"
services:
  app:
    image: node:lts-alpine3.13
    container_name: app
    volumes:
      - ./app/app:/app
    working_dir: /app
    entrypoint: >
      sh -c "npm ci
      && npm start"
    ports:
      - "80:80"