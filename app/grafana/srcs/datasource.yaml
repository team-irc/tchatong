apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: $INFLUXDB_DB
    user: $INFLUXDB_USER
    password: $INFLUXDB_ADMIN_PASSWORD
    url: http://influxdb:8086
